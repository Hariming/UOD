# Install a MATCHING torch/torchvision build for your CPU/CUDA first.
# Validated locally with torch 2.10.0+cpu / torchvision 0.25.0+cpu / Python 3.13.5.
# Earlier versions in these ranges have not been tested here.
torch>=2.4,<3
torchvision>=0.19,<1
numpy>=1.24
Pillow>=10
PyYAML>=6
